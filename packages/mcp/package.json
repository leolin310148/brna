{
  "name": "@brna/mcp",
  "version": "0.1.3",
  "description": "MCP stdio server bridging Claude/MCP clients to a brna runtime via Metro",
  "repository": {
    "type": "git",
    "url": "https://github.com/leolin310148/brna"
  },
  "type": "module",
  "bin": {
    "brna-mcp": "./dist/brna-mcp.js"
  },
  "files": [
    "dist",
    "src"
  ],
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "brna-source": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && bun build ./src/index.ts --target=node --outfile dist/index.js && bun build ./src/server.ts --target=node --outfile dist/brna-mcp.js",
    "prepack": "bun run build",
    "start": "bun run ./src/server.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@brna/core": "^0.1.3",
    "@brna/schema": "^0.1.3",
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
