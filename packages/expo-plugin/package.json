{
  "name": "@brna/expo-plugin",
  "version": "0.0.7",
  "description": "Expo Config Plugin: zero-config integration of @brna/babel-plugin and @brna/metro-plugin",
  "repository": {
    "type": "git",
    "url": "https://github.com/leolin310148/brna"
  },
  "main": "./dist/index.cjs",
  "types": "./src/index.ts",
  "files": [
    "app.plugin.js",
    "dist",
    "src"
  ],
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.cjs"
    }
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --outfile dist/index.cjs --target node --format cjs --external expo --external @expo/config-plugins --external @brna/metro-plugin --external @brna/babel-plugin",
    "prepack": "bun run build"
  },
  "peerDependencies": {
    "expo": "*"
  }
}
