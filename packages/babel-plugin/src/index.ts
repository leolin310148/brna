// This package ships as plain CommonJS JavaScript at ./index.js because Babel
// loads plugins via require() and the rest of the monorepo is ESM-only. This
// file is left as a placeholder so the workspace's TypeScript scaffolding does
// not see an empty package; do not import from it.
export {};
