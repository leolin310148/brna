// Stable id of the synthetic root node used as parent_id when deriving
// top-level node ids. Shared by snapshot capture and action target lookup
// so a `target_id` resolves through the same hierarchy that emitted it.
export const ROOT_ID = "screen:root";
